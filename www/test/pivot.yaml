name: pivot
vd: tests/pivot.vd
preview: poster="npt:0:10"
question: How do I create a pivot table?
workflow: set key column, add 'sum' aggregator and pivot on single column
timestamps:
    - 0.5
    - 3.5
    - 5.5
    - 7.5
    - 9.5
