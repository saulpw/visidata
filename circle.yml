machine:
  post:
    - pyenv global 3.4.4 3.5.3

dependencies:
    pre:
        - pip3 install -r requirements.txt
        - pip3 install requests openpyxl xlrd lxml h5py psycopg2 pyshp mapbox-vector-tile google-cloud-bigquery pypng

test:
  override:
    - dev/test.sh
