#!/usr/bin/env bash

# Pre-push hook: run unit tests and cmdlog tests before pushing

set -e

echo "Running unit tests..."
PYTHONPATH=. python -m pytest visidata/tests/

echo "Running cmdlog tests..."
dev/test.sh -j 4

echo "All tests passed."
